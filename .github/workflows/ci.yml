---
# DIAGNOSTIC: Test local vs cross-repo reusable workflows
name: CI

on:
  pull_request:
    branches: [main, development]
  workflow_dispatch:

jobs:
  # Test 1: Direct job (should always work)
  smoke-test:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Direct job works"

  # Test 2: LOCAL reusable workflow (same repo)
  local-reusable:
    uses: ./.github/workflows/test-reusable.yml

  # Test 3: CROSS-REPO reusable workflow (ci-framework)
  # Commenting out to test if local reusable works first
  # ci:
  #   uses: Claire-s-Monster/ci-framework/.github/workflows/reusable-ci.yml@v2.5.0
  #   secrets: inherit
