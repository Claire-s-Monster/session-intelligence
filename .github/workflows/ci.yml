---
# DIAGNOSTIC: Test cross-repo reusable workflow (isolated)
# Previous test confirmed: local reusable works, direct jobs work
# This test: ONLY the cross-repo call
name: CI

on:
  pull_request:
    branches: [main, development]
  workflow_dispatch:

jobs:
  ci:
    uses: Claire-s-Monster/ci-framework/.github/workflows/reusable-ci.yml@v2.5.0
    secrets: inherit
